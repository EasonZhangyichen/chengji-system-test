<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>学生成绩查询系统 - 仪表盘</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">学生成绩查询系统</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/dashboard?id=<%= studentId %>&name=<%= encodeURIComponent(studentName) %>">考试列表</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/performance?id=<%= studentId %>&name=<%= encodeURIComponent(studentName) %>">成绩趋势</a>
                    </li>
                </ul>
                <span class="navbar-text">
                    <i class="bi bi-person-circle me-2"></i><%= studentName %> (<%= studentId %>)
                    <a href="/" class="btn btn-sm btn-outline-light ms-3">退出</a>
                </span>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <div class="card shadow-sm">
            <div class="card-header bg-white">
                <h4 class="mb-0">考试列表</h4>
            </div>
            <div class="card-body">
                <% if (examList && examList.length > 0) { %>
                    <div class="list-group">
                        <% examList.forEach(exam => { %>
                            <a href="/exam/<%= exam.originalName %>?id=<%= studentId %>&name=<%= encodeURIComponent(studentName) %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                                <%= exam.displayName %>
                                <i class="bi bi-chevron-right"></i>
                            </a>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="alert alert-info">
                        暂无考试数据，请在下方管理员区域上传考试成绩Excel文件。
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <footer class="mt-5 py-3 bg-white border-top">
        <div class="container text-center text-muted">
            <small>© 2023 学生成绩查询系统</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 